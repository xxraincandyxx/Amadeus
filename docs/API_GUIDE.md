# API Guide

The canonical HTTP contract, endpoint inventory, availability matrix, event protocol, examples, security constraints, migration information, and React integration sequence are maintained in [HTTP_API.md](HTTP_API.md).

The supported external interaction surface is `/v1/sessions/*`. Historical `/agents/*`, `/stream`, `/history`, and global `/approvals/*` routes are no longer registered.
